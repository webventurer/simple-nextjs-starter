# MDX Demo Page

This is a sample MDX page that demonstrates **remark-gfm** functionality!

## What is remark-gfm?

`remark-gfm` adds support for **GitHub Flavored Markdown (GFM)** features:

### 1. Task Lists

- [x] Set up MDX in Next.js
- [x] Install remark-gfm plugin
- [ ] Create more MDX content
- [ ] Add custom components

### 2. Tables

| Feature           | Supported | Notes                  |
| ----------------- | --------- | ---------------------- |
| **Strikethrough** | ✅        | ~~old content~~        |
| **Tables**        | ✅        | Like this one!         |
| **Task Lists**    | ✅        | Interactive checkboxes |
| **Autolinks**     | ✅        | https://nextjs.org     |

### 3. Strikethrough Text

You can ~~cross out~~ text easily with double tildes.

### 4. Autolinks

Visit https://github.com/remarkjs/remark-gfm for more info!

### 5. Code Blocks with Syntax Highlighting

```javascript
// This is JavaScript code
function greet(name) {
  return `Hello, ${name}!`;
}

console.log(greet("MDX"));
```

```bash
# Terminal commands
npm install @next/mdx remark-gfm
pnpm dev
```

## Custom Components

This MDX setup also supports custom React components:

export function CustomButton({ children }) {
  return (
    <button className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
      {children}
    </button>
  );
}

<CustomButton>Click Me!</CustomButton>

## Blockquotes

> MDX is amazing because it lets you use JSX in Markdown files.
> This makes it perfect for documentation, blogs, and content sites.

---

**That's the power of MDX + remark-gfm!** 🚀
